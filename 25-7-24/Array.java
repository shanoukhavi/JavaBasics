public class Array {

    public static void main(String[] args) {
        //this is s my the array mate so indexing stattrss wth 1 matr ehsich is figven by them 
        //120 out of the 120 of the students are kept others are nto kep t
        //the addres si the same mate t o do it 
        //rom  number hostel 105 a block is the address for it mate array is given contitgous memeory location hostel 120 rom no will be there 0 or th e1 seconfd third forutrth and fifth  is ther ek hi bar rmehin hi karo bhaissab then 0 1 2 indexes are given tot all of them mate so been doin ut in the same way 
        //it stores the multipe values in s a  single variable 
        //index pson will be 4 th index 
        //now if u want to declare the aray 
        //int[] don=new arr[10]'int[]'
        //it is the int[] arr=new int[10]; there are been empty not the full the arrays eemem empty mate so been carefully doing it mate so been doing tiint[] a=new int[5kitne elelemnt chaiye doest  so u go and declare it so only brrn goof to od it it is like int[] arr=new int[10]  120 ke size ka dedo bhaiji nahi toh bhahut hi taklif hott i ha i 
        //int[] values={1,2,3,4,5,5,6,7,,8,9,9}; saize can ot be changes d mte instead of this  dammn thingwhich has been taught to do oit been carefukl is been good to od o it mate 
        // int[] myArr=new int[5];
        // myArr[0]=1;
        // myArr[1]=2;
        // myArr[2]=3;
        // myArr[3]=4;
        // myArr[4]=5;
        int[] myArr={1,2,3,4,5};
        //not required to declare the much array the this is ithe way to do it mate so ben doing it for the long period od time o been doing it is the way u do it mate so i am been doign ti in the coding ate 
        //herer the indedxe sis not required mate so bene doin g  it for the lomgh perioldd do dthe time 
        //thsis is the way u put an dkeep it in the arr amte so been doing it or the long eprriod od dthte time 
int index=0;
//thsi sis calleda sthe array traversla mat eso o been doing it idor the long peroood od the itkme 
while(index<myArr.length){
    //the elngth is the ermemebering to ido it u can do it or much operations i n it 
    System.out.println(myArr[index]);
    index++;
 //this is the way to print the array mate so been doing it for the long period od time oit been doing it is the way u do it mate so i am been doing ti in the coding ate
}
        // System.out.println(myArr[0]);
        // System.out.println(myArr[1]);
        // System.out.println(myArr[2]);
        // System.out.println(myArr[3]);
        // System.out.println(myArr[4]);
        ///herer it is the arr amte 
      //  System.out.println(myArr[5]);
        // for(int i=0;i<5;i++){
        //     System.out.println(myArr[i]);
        // }
        

    }
}
